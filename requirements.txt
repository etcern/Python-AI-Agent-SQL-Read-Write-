# --- Core ---
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0
duckduckgo-search>=6.0.0
pydantic>=2.0
rich>=13.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- File type support ---
pymupdf>=1.25.0
python-docx>=1.1.0
openpyxl>=3.1.0
Pillow>=10.0.0

# --- System info (resource profile detection) ---
psutil>=6.0.0

# --- Voice transcription (optional) ---
# Install for server-side speech-to-text (fully local, no data leaves machine):
#   pip install faster-whisper
# The browser-side Web Speech API works without this.
# faster-whisper>=1.1.0
